/**
 * 렌즈 원의 이동 경로 — 컷마다 놓이는 자리.
 *
 * 시안 컷 이미지(public/intercation-cuts)와 영상에서 원의 중심을 재서 뽑은 값이다.
 * 원은 커서를 따라오지 않는다. 컷이 넘어갈 때 다음 자리로 함께 움직이고,
 * 아카이브에 들어서면 오른쪽 화면 밖으로 빠져나간 뒤 끝까지 돌아오지 않는다.
 *
 * x, y 는 뷰포트 대비 퍼센트. 100 을 넘는 값은 화면 밖이라는 뜻이다.
 */
export type LensStop = { x: number; y: number }

/** 컷 0번부터 차례로. 목록이 끝난 뒤의 컷은 마지막 값을 그대로 쓴다. */
export const LENS_STOPS: readonly LensStop[] = [
  { x: 62.5, y: 43.5 }, // 컷 1 — LEFT BEHIND
  { x: 62.5, y: 43.5 }, // 컷 2 — 오브젝트만 떠오르고 원은 제자리 (컷 1 과 같은 값이어야 한다)
  { x: 34.2, y: 58.7 }, // 컷 3 — 구도 완성
  { x: 31.3, y: 52.4 }, // 컷 4 — 서문
  { x: 77.0, y: 54.0 }, // 아카이브 진입
  { x: 89.0, y: 47.0 },
  { x: 112.0, y: 74.0 }, // 화면 밖으로
  { x: 125.0, y: 85.0 },
]
