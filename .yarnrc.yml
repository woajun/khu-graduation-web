# 의존성을 node_modules 에 푼다.
#
# Yarn 3 의 기본값은 PnP 인데, 그러면 TypeScript 가 vite/client·@types/node 를
# 찾지 못한다(TS2688). 로컬에서는 이미 node_modules 가 있어 yarn 이 알아서
# 이 방식을 골랐지만 CI 는 빈 상태로 시작하므로 명시해야 한다.
nodeLinker: node-modules
